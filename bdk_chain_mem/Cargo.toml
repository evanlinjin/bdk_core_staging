[package]
name = "bdk_chain_mem"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bdk_core = { path = "../bdk_core", features = ["miniscript"] }
